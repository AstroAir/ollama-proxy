[pytest]
# Test discovery
testpaths = tests
python_files = test_*.py *_test.py
python_classes = Test*
python_functions = test_*

# Output options
addopts =
    --strict-markers
    --strict-config
    --tb=short
    -v
    --durations=10
    --cov=src
    --cov-report=term-missing
    --cov-report=html:htmlcov
    --cov-report=xml:coverage.xml
    --cov-fail-under=75

# Markers
markers =
    unit: Unit tests
    integration: Integration tests
    e2e: End-to-end tests
    performance: Performance tests
    load: Load tests
    security: Security tests
    contract: Contract tests
    smoke: Smoke tests
    slow: Slow running tests
    api: API tests
    database: Database tests
    network: Network tests
    auth: Authentication tests
    config: Configuration tests

# Warnings
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
    ignore::pytest.PytestUnraisableExceptionWarning

# Asyncio
asyncio_mode = auto

# Parallel execution
# Use with: pytest -n auto
# Requires pytest-xdist

# Timeout configuration requires pytest-timeout plugin
# timeout = 300
# timeout_method = thread

# Coverage options
# Exclude patterns for coverage
# These are handled in pyproject.toml

# Test environment variables would be set via pytest-env plugin
# For now, set them manually or via CI environment
